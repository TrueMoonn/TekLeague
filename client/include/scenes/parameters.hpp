/*
** EPITECH PROJECT, 2025
** TekLeague
** File description:
** parameters.hpp
*/

#pragma once

    #include "entities.hpp"

    #define TEMP_SCENE 500 +

    #define PARAM_BG static_cast<std::size_t>(TEMP_SCENE\
        MENU_BEGIN + 0)
    #define PARAM_BUTTON_BACK static_cast<std::size_t>(TEMP_SCENE\
        MENU_BEGIN + 1)
    #define PARAM_BUTTON_KEYS static_cast<std::size_t>(TEMP_SCENE\
        MENU_BEGIN + 2)
    #define PARAM_BUTTON_QUIT static_cast<std::size_t>(TEMP_SCENE\
        MENU_BEGIN + 3)
